name: Second Workflow
on:
  push:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: print present working directory
        run: pwd
      - name: long list contents with hidden files and folders
        run: ls -la
      
